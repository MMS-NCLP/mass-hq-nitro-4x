# IRO-015: TNGD-BP-006.2 — Technician Availability and Capacity — Renewed Independent Implementation Review

| Field | Value |
|---|---|
| Review Order | IRO-015 |
| Reviewer | Claude (Architecture Protection) |
| Scope | BP-006.2 Localized Corrections (travel governance, numeric validation, date-scoped remaining capacity, partial-day exception evidence, validator hardening, documentation reconciliation) |
| Canonical Review Head | `2f215eb5175a49fb2c1f9fef8c0deac04f74b1bf` |
| Corrected Artifact Commit | `cb6c10167cfbe0b3e78a3f5940e598d0304077fe` |
| Prior Reviews | IRO-013 (Codex, rejected), IRO-014 (Codex), IRO-014-AP (Claude, accepted with LCOs) |
| Correction Authority | TNGD-BP-006.2 Localized Correction Order (6 authorized corrections) |
| Review Date | 2026-08-12 |

## Artifacts Reviewed

| Artifact | Path | Lines |
|---|---|---|
| Capacity Service | `src/capacity/capacity-service.mjs` | 105 |
| Capacity Tests | `tests/capacity.test.mjs` | 111 |
| Capacity Manifest | `src/capacity/manifest.mjs` | — |
| API Inventory | `docs/bp006/API_INVENTORY.md` | — |
| Capacity Calculation Rules | `docs/bp006/CAPACITY_CALCULATION_RULES.md` | — |
| Domain and Data Model | `docs/bp006/DOMAIN_AND_DATA_MODEL.md` | — |
| Audit and Event Model | `docs/bp006/AUDIT_AND_EVENT_MODEL.md` | — |
| Revision Log | `docs/bp006/REVISION_LOG.md` | — |
| Migration Reference | `migrations/TNGD-BP-006_REFERENCE.md` | — |
| Localized Correction Order | `production/pilot/review/TNGD-BP-006.2_Localized_Correction_Order.md` | — |
| Localized Correction Report | `production/pilot/review/TNGD-BP-006.2_Localized_Correction_Report.md` | — |
| Completion Report | `production/pilot/review/TNGD-BP-006_Completion_Report.md` | — |
| Canonical Validator | `scripts/validate-repository.mjs` | 465 |

## Gate Results

| Gate | Result |
|---|---|
| `npm run check` | Exit 0 — 53/53 tests passed, build passed, validation passed |
| BP-000 through BP-005 regression | All passing |
| `git diff --check` | Clean |
| Forbidden scope scan | No BP-007 technician assignment, route optimization, dispatch operation, or dispatch-board implementation |

## Independent Boundary Probes

36 probes executed independently of the test suite. All 36 passed.

### Probe 1 — Travel distance validation (8 probes)

Verified `calculateAuthorized` rejects `travelDistanceMiles` values of `undefined`, `null`, `NaN`, `Infinity`, `-1`, and `"10"` with error message matching `/Travel distance must be a finite non-negative/`. Verified boundary: distance exactly at radius (30) produces `available: true`; distance 30.01 produces `travel-radius` reason.

### Probe 2 — Numeric limit validation (10 probes)

Verified `configureProfileAuthorized` rejects `dailyLimit` values of `undefined`, `NaN`, `Infinity`, `-Infinity`, `-1`, `1.5`, `"2"`, and `null` with error matching `/Daily capacity must be a finite integer/`. Verified `dailyLimit: 0` rejected with `/greater than or equal to 1/`. Verified valid edge: `dailyLimit: 1, sameDayLimit: 0, emergencyDailyLimit: 0` accepted without error.

### Probe 3 — Cross-date remaining capacity (2 probes)

Created 3 appointments: 2 on Aug 16, 1 on Aug 17. Configured `dailyLimit: 4, sameDayLimit: 3`. Queried for Aug 17. Verified `available: true` and `remainingCapacity: 2` (only the single Aug 17 appointment counted, not the cross-date total of 3).

### Probe 4 — Partial-day exception (2 probes)

Added training exception 13:00–14:00 on Aug 17. Verified overlapping query (13:30–14:30) receives `availability-exception` reason. Verified non-overlapping query (15:00–16:00) receives `available: true`.

### Probe 5 — Override capacity validation (8 probes)

Verified `addOverrideAuthorized` rejects `additionalCapacity` values of `undefined`, `NaN`, `Infinity`, `-Infinity`, `-1`, `1.5`, `"2"`, and `null` with error matching `/Override capacity must be a finite/`.

### Probe 6 — Travel radius validation (6 probes)

Verified `configureProfileAuthorized` rejects `travelRadiusMiles` values of `undefined`, `null`, `NaN`, `Infinity`, `-1`, and `"10"` with error matching `/Travel radius must be a finite non-negative/`.

## Requirement Verification

### R1: Travel distance required, finite, non-negative, cannot be omitted

**VERIFIED.** `governedDistance(travelDistanceMiles, "Travel distance")` at line 70 enforces `typeof number`, `Number.isFinite()`, and `value >= 0`. No default value — omission throws. Independent probes confirm all 6 invalid inputs rejected and boundary behavior correct.

### R2: Daily, same-day, emergency, override values get finite-integer validation

**VERIFIED.** `governedCapacityInteger()` applied at lines 35–37 (profile: daily with minimum 1, same-day, emergency) and line 60 (override). Each validates `typeof number`, `Number.isFinite()`, `Number.isInteger()`, and `value >= minimum`. Independent probes confirm rejection of undefined, NaN, Infinity, -Infinity, fractional, string, null, and negative values for all four fields. `dailyLimit: 0` correctly rejected; `sameDayLimit: 0` and `emergencyDailyLimit: 0` correctly accepted.

### R3: Remaining capacity counts only target-date appointments

**VERIFIED.** `assignedOnDate = assigned.filter((item) => day(item.startsAt) === date)` at line 91 filters to target date. `remainingCapacity: Math.max(0, limit - assignedOnDate.length)` at line 96 uses the filtered count. Daily-capacity enforcement at line 95 also uses `assignedOnDate.length`. Independent probe confirms cross-date appointments excluded from count.

### R4: Partial-day exceptions block overlapping intervals, leave unaffected available

**VERIFIED.** Line 88: `exceptions.some((item) => !item.startsAt || overlaps(period, { startsAt: item.startsAt, endsAt: item.endsAt }))` — all-day exceptions (no `startsAt`) always block; partial-day exceptions use `overlaps()` for interval comparison. Independent probe confirms overlapping blocked, non-overlapping available. Test suite covers this at lines 45–51.

### R5: PTO, blackout, training, administrative-hold evidence complete

**VERIFIED.** Test suite includes dedicated tests: "PTO and blackout dates block capacity" (lines 30–37), "training and administrative holds block capacity" (lines 38–43), and "partial-day exceptions block only overlapping intervals" (lines 45–51). All four exception types exercised. `addExceptionAuthorized` at line 51 enforces the valid type set.

### R6: Validator enforces all BP-006.1 and BP-006.2 corrections

**VERIFIED.** Validator lines 331–358 enforce: `governedDistance` presence, `governedCapacityInteger` presence, `assignedOnDate` variable, `limit - assignedOnDate.length` calculation, override audit metadata fields (`overrideId`, `date`, `additionalCapacity`, `reason`). Test evidence names enforced for partial-day, travel governance, date-scoped remaining capacity, and numeric validation tests. Validator passes as part of `npm run check`.

### R7: Documentation and migration references match implementation

**VERIFIED.** API Inventory describes required finite travel radius, governed integer limits, and finite non-negative override amounts. Capacity Calculation Rules detail governed numeric validation and date-scoped remaining capacity. Domain Model describes finite non-negative radii and integer limits. Audit Model includes governed override metadata. Migration Reference updated with numeric validation requirements. Revision Log records v1.2 for IRO-014/IRO-014-AP corrections. All consistent with implementation.

### R8: No regressions BP-000 through BP-005

**VERIFIED.** 53/53 tests pass covering BP-000 (security), BP-001 (foundation), BP-002 (guided intake), BP-003 (proposal), BP-004 (customer/case), BP-005 (scheduling), and BP-006 (capacity). No test failures, skips, or cancellations.

### R9: No BP-007 behavior

**VERIFIED.** No technician assignment logic, route optimization, dispatch operation, or dispatch-board implementation in any BP-006 artifact. Validator explicitly scans for and prohibits BP-007 scope. Handoff contract at line 98 declares `dispatch: "TNGD-BP-007"` as a forward reference only.

## Finding-by-Finding Resolution (IRO-014-AP LCO Recommendations)

### LCO-REC-A: NaN/Infinity validation bypass on numeric inputs

**RESOLVED.** `governedCapacityInteger()` (lines 11–16) and `governedDistance()` (lines 17–22) now enforce `typeof number`, `Number.isFinite()`, and type-appropriate constraints. Applied to all five numeric input points: travel radius, daily limit, same-day limit, emergency limit, override capacity, and query travel distance. 36 independent probes confirm complete coverage.

### LCO-REC-B: remainingCapacity cross-date counting defect and omitted travelDistanceMiles default

**RESOLVED — both sub-items:**
1. **Cross-date counting:** `assignedOnDate` (line 91) filters appointments to target date. Both `remainingCapacity` (line 96) and daily-capacity enforcement (line 95) use the filtered set. Independent probe confirms correct behavior.
2. **Omitted travelDistanceMiles:** `governedDistance(travelDistanceMiles, "Travel distance")` at line 70 has no default — omission throws immediately. Independent probe confirms `undefined` is rejected.

## Disposition

**ACCEPTED — No findings.**

All 6 authorized corrections from the BP-006.2 Localized Correction Order are fully implemented. All 9 verification requirements are satisfied. 53/53 tests pass. 36/36 independent boundary probes pass. No regressions. No BP-007 scope intrusion. Documentation and migration references are reconciled with the corrected executable contract.

BP-006 may receive Executive Acceptance.

BP-007 activation and manufacturing remain unauthorized and were neither performed nor prepared.
