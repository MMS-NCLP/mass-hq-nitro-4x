# BP-006 Permission Matrix

Tenant administrators, dispatch administrators, and managers may maintain profiles, exceptions, and overrides through `dispatch.manage`. Scheduling-capable administrators and managers may query capacity through `scheduling.manage`. Technicians cannot administer capacity. Every operation is tenant-bound and audited.
