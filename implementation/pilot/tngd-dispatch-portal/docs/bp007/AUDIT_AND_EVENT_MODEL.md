# BP-007 Audit and Event Model

The shared hash chain records work-item creation, recommendation, approval, reassignment, return, dispatch, cancellation, exception opening/resolution, actor, tenant, resource, outcome, and governing metadata. Assignment history remains append-only.
